#include <stdio.h>

void main() {
    int a = 3;
    int b = 2;
    int r = 0;
    //  r = a + b; soma
    //  r = a - b; subtração
    //  r = a * b; multiplicação
    //  r = a / b; divisão
    //  r = a % b; módulo
    //  r = -b; unario
    printf("Resultado: %d\n", r);

}